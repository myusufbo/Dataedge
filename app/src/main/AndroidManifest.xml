<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yusuf.iit.du.lms" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/CustomTheme" >
        </activity>
        <activity
            android:name=".LeaveApply"
            android:label="@string/title_activity_leave_apply"
            android:theme="@style/CustomTheme">
        </activity>
        <activity
            android:name=".Approve"
            android:label="@string/title_activity_approve"
            android:theme="@style/CustomTheme">
        </activity>
        <activity
            android:name=".Relieve"
            android:label="@string/title_activity_relieve"
            android:theme="@style/CustomTheme">
        </activity>
        <activity
            android:name=".Myleave"
            android:label="@string/title_activity_myleave"
            android:theme="@style/CustomTheme">
        </activity>
        <activity
            android:name=".Apply2"
            android:label="@string/title_activity_leave_apply"
            android:theme="@style/CustomTheme">
        </activity>
        <activity
            android:name=".Apply3"
            android:label="@string/title_activity_leave_apply"
            android:theme="@style/CustomTheme">
        </activity>
    </application>

</manifest>
